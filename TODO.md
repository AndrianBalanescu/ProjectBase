# TODO — ProjectBase

## Cycle 80 (build)

- [x] **Kanban mobile touch drag-and-drop + destroyed-instance drag guard shipped (6eddb92, PR #27).** Root-caused the cycle-79 page-error finding (`TypeError: Cannot read properties of null (reading 'lastElementChild')` inside sortable.min.js): board rerenders (SSE/session polls) recreate Sortables mid-drag; stale document-level events hit destroyed instances (el=null). Fix at the data source: idempotent `Sortable.prototype.handleEvent` guard (dead instances swallow stale events, vendored bundle untouched), `initSortable` defers re-init while `dragInProgress`, touch long-press (`delayOnTouchOnly: 180` + touchStartThreshold + fallbackTolerance), touch-grip affordance gated to `(hover:none) and (pointer:coarse)`. QA: `verify_kanban_touch_dnd.js` pass=true (desktop mouse drag, touch long-press drag, tap-opens-drawer, grip gating, 0 console/page errors/failed requests); pytest `tests/test_kanban_touch_dnd.py`; full suite **609 passed**; AGENTS.md drift fixed 558/47→559/48 (drift guard green); iBrowse visual audit PASS (0 console errors, 0 network failures, no click blockers, overflow = intended board column scroll, drag handles 3/3 on filtered cards). Board card PB-10532 (audit comment posted at cycle end).

## Cycle 78 (build)

- [x] **Stale DEV_MODE file drift identified (cycle 78 pass 11).** Two DEV_MODE files exist: the authoritative one the current engine reads/writes (agents-hub/DEV_MODE = 'build', mtime = cycle-78 heart phase) and a stale pre-unification leftover (.flow-work/DEV_MODE = 'harden', mtime Aug 29) that no current code path reads (flow was unified into agents-hub in 5c572b5; supervisor resolves operator_root(flow_home)=FLOW_HOME=agents-hub). The cycle-78 env snapshot correctly reported build. Left in place (workspace file; removal is a human call), documented here so future agents don't misread it. Candidate cleanup: delete the stale .flow-work/DEV_MODE when the human confirms no pre-unification tooling reads it.

- [x] **List View semantic column sorting + subtask progress column (bcfdf1e, pushed).** Pts sorted lexicographically (10 above 9); due_date sorted as raw strings; status/priority sorted alphabetically instead of workflow/severity order. Fixed at the component data-source: numeric estimate sort, chronological due (undated last in BOTH directions), statusRank by Kanban workflow order, priorityRank by severity, new sortable Subs column (inline completion bar + done/total, ratio-then-count sort), empty-state colspan 8->9. CSS rebuilt via build_css.sh (css-sync guard green). QA: real-browser E2E `scripts/qa/verify_listview_sorting.js` (login -> list route -> header clicks, asserts each order, 0 console errors / 0 failed requests, pass=true); adversarial logic assertions `scripts/qa/verify_listview_sort_logic.js`; pytest guard `tests/test_listview_sort_logic.py`. iBrowse audit (homelab, LAN URL): SUCCEEDED, 15/15 steps, zero anomalies (console_errors/slow_requests/aborted_requests/broken_links all empty). Board card PB-10531.

## Cycle 77 (harden)

- [x] **Cycle-76 veto investigated and proven false-positive (the 3rd investigated-and-disproven veto after cycles 72 and 75; the 8th zero-findings veto overall — 10 FAIL(124) kills across cycles 64-76, 8 writing immediate vetoes at 66/67/69/70/72/74/75/76).** The cycle-76 inspect session was killed at its 600s timeout (supervisor flow.log: `[flow] inspect FAIL(124)` — GNU timeout exit code; the kill came from `PHASE_TIMEOUT_INSPECT=600` pinned in flow.conf, not merely the code default), before writing any findings: `/tmp/flow-inspect-result.json` is stale from cycle 74, the cycle-76 inspect log has no final verdict JSON (only the skill-template text and a mid-run activity-orphan check that came back clean: 237 rows, 0 dangling refs), yet `.flow-audit-veto.json` holds a bare `FAILED_AUDIT`. Programmatic attribution across the whole supervisor log shows `inspect FAIL(124)` + immediate veto at cycles 66, 67, 69, 70, 72, 74, 75, 76 (and un-vetoed timeout kills at 64, 68) — this is a chronic inspect-timeout pattern, not a one-off. Substance re-verified independently: full suite **606 passed / 0 failed** (196s) against live 8120, main==origin/main, repo clean, 0 open PRs. Same root cause as cycle 75: `flow/supervisor.py` `inspect_verdict()` log fallback (engine-bug item below remains open).
- [x] **AGENTS.md test-count drift fixed at the class level** (6b2c82d): the directory-map line claimed "562 tests across 46 files" while the drift-guard-locked **Tests:** bullet said 556 — a conflicting stale claim that misled agents. Fixed both claims to the real surface (557) and added `test_agents_no_conflicting_count_claims` so the guard now rejects ANY 'N tests across M files' claim that disagrees with the tree, instead of only checking the first regex match.
- [x] **flomaster engine bug FILED AS PR (2026-09-06, cycle 78):** `flow/supervisor.py` `inspect_verdict()` log fallback matched `FAILED_AUDIT|BINDING_VETO` tokens anywhere in the inspect log, including quoted skill instructions and stale result JSON from a previous cycle — cycles 72/75/76 produced vetoes with zero findings. Fix on branch `fix/inspect-verdict-false-vetoes` (commit e25f26c): log-only veto now requires a current-cycle `/tmp/flow-inspect-result.json` corroboration when the supervisor passes a known cycle; legacy no-cycle callers keep the loud behavior. Regression tests included (5 new). **PR: https://github.com/AndrianBalanescu/agents-hub/pull/4**

- [x] **Calibration Hat considered (cycle 77, 77%5=2 but suite 100% green):** STOP polishing. The veto-disproval + drift-guard work was the last legitimate harden item; next cycles must NOT continue the 72/75/76 polish loop. Milestone advance (self-hosting Docker polish, multi-project sync, SSE perf, docs/OpenAPI per calibration focus) is gated on the human/inspect clearing the veto and CI billing fix — those are the two blockers to record.
- [x] **flomaster engine bug #2 FILED AS PR (2026-09-06, cycle 78, same PR #4):** `flow/fleet.py:261` looked up the example config at `engine_root()/'config'/flow.conf.example` but the file lives at `engine_root()/'flow'/'config'/flow.conf.example` (empirically verified: old path is_file()=False). Every `flow new` silently seeded the bare stub config. Fixed: probe the real flow/config/ path first, legacy root as fallback, plus 2 regression tests. **PR: https://github.com/AndrianBalanescu/agents-hub/pull/4**
- [ ] **GitHub Actions billing blocker (human required):** CI on ProjectBase fails every push since at least 3fb62b1 (cycle 76) with "The job was not started because recent account payments have failed or your spending limit needs to be increased" — both jobs (test, Docker fresh-container boot) never start, so CI has been red-by-billing, not red-by-code. Local full suite is green (607/607). Needs human action in GitHub Billing & plans; agents cannot resolve payments.
## Cycle 76 (harden)

- [x] **Cycle-75 veto investigated and proven false-positive.** The inspect session was killed at timeout before writing `/tmp/flow-inspect-result.json`; the supervisor's log-fallback regex matched `FAILED_AUDIT`/`BINDING_VETO` tokens that appear in quoted skill instructions inside the log. Substance check: full suite 606/606 green on live 8120, all cycle-74/75 findings already resolved by PR #26 (6205bb2).
- [x] **Inspect's open anomaly root-caused** (activity record 404-by-id on instrumented instances): PocketBase 0.39.11 first-boot quirk — on a fresh data dir, the records API is blind to rows in migration-created collections (even API-written ones) until the process restarts. Minimal repro: saving the same collection twice within migrations breaks first-boot visibility. Live instance unaffected. Fix shipped in 8067a80 (`scripts/serve-firstboot.sh` wired into Dockerfile ENTRYPOINT + systemd unit) and ec4cc4e (`__SCRIPTS_DIR__` placeholder).
- [x] **Data hygiene:** 3 orphan activity rows (issue refs pointing at deleted fixture issues) deleted via API; verified 0 remaining and cascadeDelete works (probe created→deleted issue with activity rows gone).
- [ ] **flomaster engine bug to file as PR (engine read-only this phase):** `flow/supervisor.py` `inspect_verdict()` log fallback matches `FAILED_AUDIT|BINDING_VETO` tokens anywhere in the inspect log, including quoted skill instructions and stale result JSON from a previous cycle. Two concrete fixes: (1) exclude matches inside quoted skill text / markdown fences, (2) ignore result JSON whose `cycle` field is older than the current cycle instead of only "matching current cycle counts".
- [ ] Upstream: report PB 0.39.11 first-boot records-invisibility to PocketBase (minimal repro: two-migration double-save of a collection + seeded row; API rules evaluate as anonymous on first boot, heal on restart).

## Standing rules

- Frontend changes require iBrowse QA (0 console errors / 0 4xx-5xx) before HAD_WORK.
- Every cycle that touches real code ends with a structured ProjectBase audit comment.
- DEV_MODE=harden: bugfixes only, no new features.